; These would sort in a non-deterministic order, because they all have the same
; number of references.
@a jmp %a
@b jmp %b
@c jmp %c
@d jmp %d
@e jmp %e
@f jmp %f
@g jmp %g
@h jmp %h
@i jmp %i
@j jmp %j
